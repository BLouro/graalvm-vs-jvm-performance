apiVersion: 1

providers:
  - name: "Performance"
    orgId: 1
    folder: "JVM vs Native"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards